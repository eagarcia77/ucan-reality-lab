server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location = /healthz {
    access_log off;
    add_header Content-Type text/plain;
    return 200 'ok';
  }

  location = /authoring.html {
    add_header Cache-Control "no-store, no-cache, must-revalidate" always;
    sub_filter_once on;
    sub_filter_types text/html;
    sub_filter '</body>' '<script src="/image-ai.js?v=77"></script><script src="/workspace-sync.js?v=77"></script></body>';
    try_files $uri =404;
  }

  location ~* \.(js|html)$ {
    add_header Cache-Control "no-store, no-cache, must-revalidate" always;
    try_files $uri $uri/ /index.html;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }

  gzip on;
  gzip_types text/plain text/css application/javascript application/json image/svg+xml;
}